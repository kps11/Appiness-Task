#Appiness Task

There are two servers 

 1. UI (React Server) 3000 port
 2.Backend (Node Server) 5000 port

#There is no DB here as there is one user to login and employee data as a json data 
# There is no password masking in backend . There is a String matching condition(We can use JWT authenticate token for login)
